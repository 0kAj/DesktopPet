<UserControl x:Class="DesktopPet.UI.GameWindows.customControls.TimerDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="1" HorizontalAlignment="Center" VerticalAlignment="Top">
        <TextBlock x:Name="Timer_Tb"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Center"
                   RenderTransformOrigin="0.5,0"
                   Text="{ Binding DisplayTime }"
                   FontSize="50"
                   Foreground="{ Binding TimerColor }">
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="2" Color="Black" />
            </TextBlock.Effect>
            <TextBlock.RenderTransform>
                <ScaleTransform x:Name="ScaleT" ScaleX="1" ScaleY="1" />
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</UserControl>