<UserControl x:Class="DesktopPet.UI.GameWindows.customControls.PauseMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="600" d:DesignHeight="400"
             Width="600" Height="400">
    <UserControl.Resources>
        <ResourceDictionary Source="../../ResourceDictionaries/DesktopPetTheme.xaml" />
    </UserControl.Resources>
    <Grid Background="#AA000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Border Style="{StaticResource CardBorder}" Width="400" Height="300" CornerRadius="10">
            <StackPanel Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White">
                <TextBlock Text="Pause" FontSize="50" FontWeight="Bold" HorizontalAlignment="Center" />
                <Button Style="{StaticResource AccentButtonAqua}" FontSize="20" Content="Resume" x:Name="ResumeButton"
                        Margin="5" />
                <!-- <Button Content="Restart" Name="RestartButton" Margin="5" /> -->
                <Button Style="{StaticResource AccentButtonRed}" Foreground="Wheat" FontSize="20" Content="Leave"
                        x:Name="LeaveButton" Margin="5" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>